# (account)

Se implementa en la **Fase 4**: `profile/page.tsx`, `stats/page.tsx`, `history/page.tsx`.
Rutas protegidas: requieren sesión (ver `verifySession` en una futura Data Access Layer de `src/features/auth`).
