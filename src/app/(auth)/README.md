# (auth)

Se implementa en la **Fase 4**: `login/page.tsx`, `signup/page.tsx`, `auth/callback/route.ts`,
usando `@supabase/ssr` (ver src/lib/supabase/server.ts) y Server Actions con `useActionState`.
