# profile-stats

Se implementa en la **Fase 4**.

- `domain/streak.ts` — cálculo de racha diaria (incrementa/rompe según `last_played_date`) + tests.
- `domain/ranking.ts` — agregación para el leaderboard.
- `actions/`, `components/`.
